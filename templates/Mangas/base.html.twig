<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
         <link rel="stylesheet" href="./css/style.css">
        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" integrity="sha384-JcKb8q3iqJ61gNV9KGb8thSsNjpSL0n8PARn9HuZOnIxN0hoP+VmmDGMN5t9UJ0Z" crossorigin="anonymous">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body>
        <header>
            <nav>
                <a class="nav-link active" href="{{path("home")}}">Home</a>
                <a class="nav-link" href="{{path('mangasList')}}">Listado Mangas</a>
                <a class="nav-link" href= "{{path("characterslist")}}">Listado Personajes</a>
                <a class="nav-link" href= "{{path("createManga")}}">Crear Manga</a>
                <a class="nav-link" href= "{{path("createUser")}}">Crear Cuenta</a>
                <a class="nav-link" href= "{{path("app_login")}}">Login</a>
            </nav>
            {% if is_granted("ROLE_USER") %}
                <h3>Usuario: </h3>
                <p>{{app.user.name}}</p>
            {% endif %}
        </header>

        {% block body %}{% endblock %}
    </body>
</html>
